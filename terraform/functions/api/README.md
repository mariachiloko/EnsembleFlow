# API Handler

This folder contains the Lambda handler for the EnsembleFlow API.

## Local Setup

Run `npm install` in this folder before packaging or deploying so the Lambda source directory includes the installed dependencies.

## What It Handles

- health checks
- profile reads and updates
- ensemble reads, creates, and updates
- presigned upload URL creation

